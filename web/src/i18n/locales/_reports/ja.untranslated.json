{
  "Applied to models without an explicit price or ratio (e.g. when self-use mode is enabled).": "Applied to models without an explicit price or ratio (e.g. when self-use mode is enabled).",
  "By request model": "By request model",
  "By upstream model": "By upstream model",
  "Default ratio for unset models": "Default ratio for unset models"
}
